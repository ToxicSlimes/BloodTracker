# Domain — Техдолг и улучшения

## 🔴 Высокий приоритет

### 1. Все сущности в одном файле
`Models/Entities.cs` содержит **все** доменные классы, enum-ы и record-ы (~230 строк). Для читаемости стоит разнести по файлам:
- `Models/Entity.cs` — базовый класс
- `Models/Analysis.cs`
- `Models/Course.cs`, `Models/Drug.cs`, `Models/IntakeLog.cs`, `Models/Purchase.cs`
- `Models/Workout/` — `WorkoutProgram.cs`, `WorkoutDay.cs`, `WorkoutExercise.cs`, `WorkoutSet.cs`
- `Models/Catalog/` — `DrugCatalogItem.cs`, `Manufacturer.cs`, `ExerciseCatalogEntry.cs`
- `Models/Auth/` — `AppUser.cs`, `AuthCode.cs`
- `Models/Enums/` — все enum-ы

### 2. Нет доменной валидации
Сущности не содержат никакой валидации. Например:
- `Purchase.Quantity` может быть отрицательным на уровне домена
- `Purchase.Price` — аналогично
- `Course.EndDate` может быть раньше `StartDate`
- `Analysis.Label` может быть пустой строкой

Валидация есть только в Application-хендлерах (и то частично).

### 3. Нет value objects
`ReferenceRange` — единственный record. Кандидаты на value objects:
- `Dosage` (сейчас просто `string?`)
- `Money` (для `Purchase.Price`, сейчас `decimal` без валюты)
- `DateRange` (для `Course.StartDate`/`EndDate`)

## 🟡 Средний приоритет

### 4. Inconsistent подход к каталожным сущностям
- `DrugCatalogItem`, `Manufacturer`, `ExerciseCatalogEntry` — **не** наследуют `Entity`, имеют `string Id` вместо `Guid`
- `AppUser`, `AuthCode` — тоже не наследуют `Entity`, но дублируют его поля (`Id`, `CreatedAt`)
- Стоит или ввести второй базовый класс (`CatalogEntity`), или унифицировать

### 5. `Drug.Amount` — неясная семантика
`Amount` — это `string?`. Непонятно чем отличается от `Dosage` (тоже `string?`). Нужен комментарий или рефакторинг.

### 6. `IntakeLog.DrugName` — денормализация
`DrugName` дублирует `Drug.Name`. Если имя препарата изменится — лог останется со старым значением. Возможно, это осознанное решение (исторические данные), но нигде не задокументировано.

### 7. Нет доменных событий
При создании/удалении сущностей нет событий. Если потребуется реактивность (уведомления, пересчёт) — придётся добавлять.

## 🟢 Низкий приоритет

### 8. `DrugCatalogItem` перегружен
25+ свойств в одном классе. Можно вынести группы:
- Фармакология: `HalfLife`, `DetectionTime`, `AnabolicRating`, `AndrogenicRating`
- Описание: `Description`, `Effects`, `SideEffects`
- Мета: `IsPopular`, `SortOrder`, `PubMedSearchTerm`

### 9. Отсутствуют XML-комментарии у enum-ов
Enum-значения не документированы. `DrugSubcategory.General`, `DrugCategory.DopamineAgonist` — полезно было бы добавить пояснения.
