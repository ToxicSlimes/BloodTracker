# 🏰 BloodTracker Dungeon UI — Дизайн-решения (Design Decisions)
## Ответы на нерешённые вопросы аудита кодовой базы
### Дата: 2026-02-14 | Версия: 1.0

---

## Контекст

Аудит кодовой базы выявил 6 архитектурных вопросов, требующих дизайн-решений перед началом
разработки. К ним добавлен 7-й вопрос — о навигационной карте. Все решения привязаны к
визуальному эталону — **Asciipocalypse** (wonrzrzeczny/Asciipocalypse), C#/MonoGame движок
с полной программной растеризацией треугольников, где каждый пиксель заменён ASCII-символом,
подобранным по яркости и глубине через z-buffer.

### Визуальный эталон — уровень качества Asciipocalypse

Asciipocalypse реализует **dense ASCII rendering**: всё изображение на экране (сцена, UI,
монстры, стены) сначала проходит через 8-bit цветовую консольную симуляцию (`Console.cs`),
затем рисуется покадрово (`ASCII_FPS.cs Draw()`). Символы подбираются по z-buffer глубине —
ближние объекты получают плотные символы (`█▓▒#@%`), дальние — разреженные (`·:;,.+-`).
Цвет каждого символа индивидуален. Текстуры стен, пола и потолка тоже проходят через
ASCII-маппинг, давая ощущение кирпичной кладки, камня, лавы — целиком из символов.

На скриншоте из игры видно: красные фигуры монстров-магов с зелёными светящимися глазами,
каждая складка плаща состоит из `x`, `*`, `+`, `#`, `@` с индивидуальными цветами от
тёмно-бордового до ярко-красного. Фон — стены из серых `*`, `+`, `.`, `:` с градиентом
затемнения по расстоянию. Это **не схематичный ASCII-арт** — это полноценный 3D-рендеринг
в символах.

**Именно этот уровень детализации — target quality для BloodTracker Dungeon UI.**

### Asciipocalypse как основа движка

Asciipocalypse берётся за основу не только как визуальный эталон, но и как **техническая
референсная архитектура**. В его кодовой базе уже реализованы:

- **Растеризатор треугольников** (`Rasterizer.cs`) с z-buffer для определения видимых поверхностей
- **Консольная симуляция** (`Console.cs`) — буфер символов с индивидуальными цветами
- **Портальный рендеринг** — рисуются только видимые из текущей комнаты стены (оптимизация)
- **Текстуры стен/пола** через ASCII-маппинг яркости
- **Спрайты** с 4-направленными видами и z-buffer окклюзией
- **Процедурная генерация уровней** с комнатами, коридорами и дверями

Для нашего веб-приложения мы адаптируем эти паттерны в TypeScript/CSS: портальный рендеринг
переводится в CSS `perspective` + `transform-style: preserve-3d`, консольная симуляция — в
AnsiCell buffer (ТЗ секция 6B), текстуры стен — в процедурные CSS-паттерны из Unicode блоков.
Готовые ASCII-ассеты стен, дверей и коридоров из Asciipocalypse служат стартовой точкой
для шаблонов наших комнат.

---

## Решение 1. ASCII-заголовок и скелеты → Гравировка-барельеф на стене главного зала

### Суть решения

Текущий ASCII-арт логотип **BloodTracker** и парные скелеты-стражи из `ascii-art.ts`
**выгравированы на каменной стене** главного зала как монументальный барельеф. Это не плоский
текст на стене — это трёхмерная гравировка, утопленная в камень, с тенями от факелов,
которые подчёркивают глубину высечки.

### Уровень детализации скелетов — уже на месте

Текущие скелеты в файле `wwwroot/js/effects/ascii-art.ts` — это **336 символов в ширину**,
сотня строк в высоту, с полным набором шейдинг-символов: `-` для пустоты/фона, `=+*` для
переходных зон и полутонов, `#%@` для плотных костных структур. Каждая фаланга пальцев,
каждое ребро, каждый зуб в черепе — индивидуально прорисованы через градиент плотности
символов.

Конкретно в арте реализовано: два симметричных скелета с детализированными черепами (глазницы
из тёмных `.·:`, контуры из ярких `#%@`), грудные клетки с рёбрами (дуги из `*#%` с
плавным градиентом яркости от центра к краям), позвоночные столбы, тазовые кости, руки с
индивидуальными фалангами каждого пальца — и всё это обрамлено текстурными узорами из
`%#*+=`. Между скелетами — пространство для центрального логотипа.

Это **уже уровень Asciipocalypse** по плотности и детализации. Менять сам арт не нужно —
задача в том, чтобы правильно встроить его в 3D-сцену подземелья. Функция `scaleAsciiSkull()`
уже масштабирует арт под ширину контейнера через `requestAnimationFrame`, эта логика
сохраняется и адаптируется для viewport главного зала.

### Композиция на центральной стене зала

Скелеты располагаются по бокам от логотипа BloodTracker на **дальней центральной стене**
главного зала — как стражи гробницы. Над композицией — факел, от которого на гравировку
падает мерцающий свет. Тени от букв и скелетов анимируются через CSS `text-shadow` /
`@keyframes` — факел мерцает, тени плавают по каменной кладке.

```
Композиция на дальней стене (что видит игрок):

              🔥 (факел, мерцает @keyframes)
    ╔════════════════════════════════════════╗
    ║▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓║  ← каменная стена (фон)
    ║▓▓ 💀 скелет-страж ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓║
    ║▓▓ (левый, из      ▓▓ BLOODTRACKER ▓▓▓║  ← логотип между скелетами
    ║▓▓  ascii-art.ts)  ▓▓  ASCII LOGO  ▓▓▓║
    ║▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓║
    ║▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ 💀 скелет-страж ║
    ║▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ (правый,       ║
    ║▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  зеркальный)   ║
    ╚════════════════════════════════════════╝
         тени плавают от мерцания факела
```

### Техническая реализация гравировки

Гравировка реализуется через **два наложенных слоя ASCII**. Первый слой — кирпичная текстура
стены из символов `▓▒░#=|-` (серо-коричневые тона, `--color-wall-stone`). Второй слой —
вдавленный слой гравировки поверх первого. Эффект «вдавленности» создаётся через `text-shadow:
1px 1px 0 var(--color-shadow)` на символах гравировки (тень вниз-вправо = свет сверху-слева
от факела) и чуть более тёмный `background-color` за символами арта относительно окружающей
стены.

Мерцание факела — чистый CSS `@keyframes` на `text-shadow` и `opacity` (как описано в ТЗ
секция 6C): `.torch-glow { text-shadow: 0 0 8px #f80, 0 0 16px #f60; }` с анимацией
`torch-flicker { 0% { opacity: 0.9 } 50% { opacity: 1.0 } 100% { opacity: 0.85 } }`.
Никакого JavaScript для анимации — всё на CSS compositor thread, 60 FPS бесплатно.

---

## Решение 2. Color Picker → Факел-переключатель цветовой темы

### Суть решения

Color picker превращается в **интерактивный факел** на боковой стене главного зала. Клик по
факелу меняет цвет пламени, и вместе с ним перекрашивается вся цветовая тема подземелья.
Это не просто кнопка смены темы — это полноценный интерактивный объект в 3D-сцене.

### Механика взаимодействия

Факел висит на правой (или левой) боковой стене зала на видном месте — между дверьми. При
наведении курсора пламя ярче разгорается (hover: усиление `text-shadow` и увеличение
`font-size` огненных символов на 10-15%). При клике вокруг факела раскрывается **огненное
кольцо выбора цвета** — полукруг из 5 цветных секторов. Каждый сектор — другой цвет пламени,
реализованный как маленький ASCII-огонь с соответствующим оттенком. Пользователь кликает на
нужный цвет, факел вспыхивает этим цветом (короткая flash-анимация на 0.3с), кольцо
схлопывается, и вся тема плавно перекрашивается через CSS `transition` на custom properties.

```
Состояния факела:

[idle]              [hover]              [active — выбор цвета]

    )                  ))                  🔴 кровь
   )|}               ))|})              🟢         🟡
   (|)               ((|))            яд     (|)    золото
    |                  ||                  🔵    🟣
    Ħ                  Ħ                лёд    тень
    |                  |
```

### Маппинг цветов на темы

Каждый цвет факела переключает набор CSS custom properties (из ТЗ секция 13). Переход
между темами — плавный, `transition: color 0.6s, text-shadow 0.6s, background-color 0.6s`
на корневом элементе `:root`.

```
Цвет факела    →    Тема подземелья
─────────────────────────────────────────────────────────
🔴 Красный         Кровавая тема (DEFAULT).
(default)          --color-primary: #ff3333
                   Огонь красный, стены тёплые, кирпич красноватый.
                   Это родная тема BloodTracker — кровь, тестостерон.

🟢 Зелёный         Ядовитая тема.
                   --color-primary: #33ff66
                   Факелы зелёные, стены покрыты мхом/слизью,
                   двери с зеленоватым свечением. Болотный данж.

🔵 Синий            Ледяная тема.
                   --color-primary: #3366ff
                   Холодные факелы, стены с инеем,
                   двери с голубым свечением. Ледяная пещера.

🟡 Жёлтый/Золотой  Золотая тема.
                   --color-primary: #ffcc33
                   Тёплый свет, богатые стены, позолоченные двери.
                   Королевское подземелье.

🟣 Пурпурный       Теневая тема.
                   --color-primary: #9933ff
                   Мистические факелы, тёмные стены с фиолетовым отливом.
                   Некромантское подземелье.
```

### Техническая деталь

Текущий color picker сохраняет выбор в `localStorage`. Факел-переключатель использует тот же
механизм — при загрузке страницы читаем `localStorage.getItem('dungeon-theme')`, применяем
соответствующий набор CSS variables на `:root`. Переключение сводится к одной строке:
`document.documentElement.setAttribute('data-theme', colorName)`, а все цвета описаны в CSS
через `[data-theme="green"] { --color-primary: #33ff66; --color-torch: #2d8; ... }`.

---

## Решение 3. Информация о пользователе → Не применимо (пропускаем)

### Суть решения

В текущей кодовой базе **нет блока информации о пользователе** (имя, аватар, статус) —
этих данных просто не существует в UI. Поэтому на данном этапе этот вопрос закрывается
как «не применимо».

### Задел на будущее

Если в будущем появится необходимость отображать пользовательскую информацию (имя, уровень,
статистику), она будет реализована как **каменная табличка на стене** главного зала — рядом
с входом (справа от точки появления игрока). Табличка — высеченная в камне надпись с рамкой
из box-drawing символов (`╔═╗║╚═╝`), содержащая имя пользователя и ключевые метрики.
Реализация: `<div class="wall-plaque">` с `position: absolute` внутри 3D-сцены, текст из
Unicode box-drawing, шрифт JetBrains Mono. Но пока — **не реализуется**.

---

## Решение 4. Быстрые действия → Светящиеся руны на камнях пола

### Суть решения

Быстрые действия (например «добавить анализ», «записать приём», «открыть дашборд»)
реализуются как **светящиеся руны-надписи на каменных плитах пола** главного зала. Это
магические символы, высеченные в камне, которые мягко пульсируют цветом текущей темы.
Они работают как **быстрая навигация** — альтернатива кликам по дверям.

### Визуальная концепция

Представь пол главного зала из каменных плит. На нескольких плитах — светящиеся
руноподобные надписи. Они не кричат о себе (это не неоновые вывески), а мягко мерцают,
как вкрапления магии в камне. При наведении курсора руна ярко вспыхивает, текст надписи
становится отчётливым и читаемым. Это создаёт ощущение **магических телепортационных
кругов** — ты стоишь в зале, а вокруг тебя на полу начерчены светящиеся символы перемещения.

```
Вид пола главного зала (схематично, вид сверху):

    ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒╔══════╗▒▒▒▒▒▒▒▒▒▒╔══════╗▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒║⊕АНАЛИЗ║▒▒▒▒▒▒▒▒▒▒║⊕ КУРС ║▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒╚══════╝▒▒▒▒▒▒▒▒▒▒╚══════╝▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒▒▒▒▒▒╔════════╗▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒▒▒▒▒▒║⊕ДАШБОРД ║▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒▒▒▒▒▒╚════════╝▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒
    ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒

    ⊕ = руна-символ, пульсирует светом текущей темы
```

### Набор быстрых действий (руны)

```
Руна             Действие                  Расположение на полу
──────────────────────────────────────────────────────────────────
⊕ ДАШБОРД        Телепорт в тронный зал     Центр пола (самая большая руна)
⊕ + АНАЛИЗ       Открыть форму добавления   Левая часть пола
⊕ КУРС           Перейти в алхимическую     Правая часть пола
⊕ СРАВНЕНИЕ      Зеркальный зал             Ближний левый угол
```

### Техническая реализация

Каждая руна — `<div class="floor-rune" data-action="dashboard">` с `position: absolute`
на плоскости пола (CSS `transform: rotateX(75deg)` для перспективного наклона, так что
надпись лежит «плашмя» на полу). Текст руны — Unicode символы в JetBrains Mono. Пульсация
реализуется чистым CSS без JavaScript:

```css
.floor-rune {
    animation: rune-glow 3s ease-in-out infinite;
    cursor: pointer;
}

@keyframes rune-glow {
    0%   { text-shadow: 0 0 4px var(--color-primary); opacity: 0.6; }
    50%  { text-shadow: 0 0 12px var(--color-primary),
                        0 0 24px var(--color-primary); opacity: 1.0; }
    100% { text-shadow: 0 0 4px var(--color-primary); opacity: 0.6; }
}

.floor-rune:hover {
    text-shadow: 0 0 20px var(--color-primary),
                 0 0 40px var(--color-primary);
    opacity: 1.0;
    animation: none; /* при hover — статично яркая */
}
```

При клике — навигация работает так же, как клик по двери (переход камеры к комнате), но
без анимации открытия двери — мгновенный «телепорт» с flash-эффектом (экран на 0.2с
заливается цветом руны через `background: var(--color-primary)`, потом fade-in комнаты).

---

## Решение 5. Страница логина → Вход в подземелье (ворота)

### Суть решения

Страница логина **обязательно** оформляется в стилистике подземелья. Это **ворота в
подземелье** — массивные каменные двери с замком. Ввод логина/пароля — это «вставить ключ
и повернуть».

### Визуальная концепция

Пользователь видит полноэкранную сцену: перед ним — огромные каменные ворота подземелья
в конце тёмного каменного туннеля. Над воротами — два факела по бокам. Между факелами —
высеченное в камне название «BLOODTRACKER» в стиле барельефа (как превью того, что ждёт
внутри главного зала). По бокам ворот — фрагменты скелетов-стражей как тизер основного
арта. Вокруг — тёмный каменный свод туннеля из символов `░▒▓` с градиентом затемнения
к краям экрана.

Форма логина интегрирована в ворота: поле логина — это «замочная скважина» (input-поле
в рамке из box-drawing символов), поле пароля — ещё одна скважина ниже, кнопка «Войти» —
это массивный засов по центру ворот.

```
Визуальная схема экрана логина:

╔══════════════════════════════════════════════════════════╗
║░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░░░░░░░░   BLOODTRACKER   ░░░░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░░░░░░░░░ (гравировка)  ░░░░░░░░░░░░░░░░░░░░░░░║
║░░░░░░🔥░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░🔥░░░░░░░░░║
║░░░░░░░░░░╔══════════════════════════╗░░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓                      ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓  ┌────────────────┐  ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓  │ 🔑 Login:      │  ▓▓║  ← замочная скважина
║░░░░░░░░░░║▓▓  └────────────────┘  ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓  ┌────────────────┐  ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓  │ 🔐 Password:   │  ▓▓║  ← вторая скважина
║░░░░░░░░░░║▓▓  └────────────────┘  ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓                      ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓  ╔══════════════╗   ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓  ║  ⚔ ВОЙТИ ⚔   ║   ▓▓║  ← засов-кнопка
║░░░░░░░░░░║▓▓  ╚══════════════╝   ▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░║▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓║░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░╚══════════════════════════╝░░░░░░░░░░░░░░░░░░░║
║░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░║
╚══════════════════════════════════════════════════════════╝
```

### Анимация входа

После успешной аутентификации запускается кинематографичная анимация **открытия ворот**:
ворота медленно расходятся в стороны (`transform: translateX()` с `transition: 1s ease-in`),
из растущей щели бьёт свет факелов из главного зала (нарастающий `text-shadow` + `background`
свечение), камера плавно въезжает внутрь (`perspective` + `translateZ`) — и пользователь
оказывается в главном зале подземелья перед скелетами-стражами и дверями комнат.

Эта анимация — **первое впечатление** от приложения. Длительность: ~1.5-2 секунды.
Полная последовательность: темнота → факелы загораются → ворота раздвигаются → свет из
зала → камера въезжает → главный зал. Никакого мгновенного перехода.

### Техническая деталь

Страница логина — отдельный `<div class="gate-page">` с тем же CRT/noise/vignette overlay,
что и основной UI (overlay-эффекты из текущей кодовой базы сохраняются). Форма логина
остаётся стандартными `<input type="text">` и `<input type="password">` для совместимости
с менеджерами паролей и автозаполнением браузера. Визуальная обёртка — чисто CSS, никаких
кастомных input-компонентов.

---

## Решение 6. Скрытая дверь админки → Сломанный камень в стене

### Суть решения

Вход в админку — **сломанный/треснувший камень** в стене главного зала, который отличается
от остальных камней едва заметной трещиной. При клике на него камень вдавливается (как
механическая кнопка), часть стены отъезжает в сторону, и открывается потайной проход в
комнату администратора.

### Визуальная детализация — уровень Asciipocalypse

Это ключевой элемент, который должен быть реализован на **максимальном уровне детализации
ASCII**. Сломанный камень — это не один символ `X` на стене. Это участок стены размером
примерно 5×3 символа, где кирпичная кладка имеет характерную трещину из символов `/`, `\`,
`─`, `╱`, `╲`. Трещина отличается от обычных случайных потёртостей стены тем, что она
чуть геометричнее — она идёт по контуру отдельной каменной плиты, намекая что эта плита
может двигаться отдельно от стены.

```
Обычная стена (для сравнения):
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
▓█▓▒█▓▓█▓▒▓▓█▓▓▒▓██▓▓▒█▓▓██▓▓
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
▓▒██▓▓█▓▓▓▒█▓██▓▓▓█▓▓▒█▓▓█▓▓▓
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓

Участок со сломанным камнем (едва заметная разница):
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
▓█▓▒█▓▓█▓▒▓▓█┌─╲▓██▓▓▒█▓▓██▓▓
▓▓▓▓▓▓▓▓▓▓▓▓│░░│▓▓▓▓▓▓▓▓▓▓▓▓  ← тонкая геометричная трещина
▓▒██▓▓█▓▓▓▒█└─╱▓██▓▓▓█▓▓▒█▓▓▓     по контуру одного камня
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
```

### Механика взаимодействия — четыре фазы

**Фаза 1 — Обнаружение.** Камень едва отличается от соседних. Курсор при наведении **не
меняется** (нельзя палить секрет через `cursor: pointer`). Единственный хинт — символы
трещины едва заметно пульсируют с очень медленным периодом: `animation: crack-hint 8s
ease-in-out infinite`. Пульсация настолько медленная и незначительная (изменение `opacity`
от 0.85 до 0.95), что случайный взгляд её не заметит. Нужно всматриваться.

**Фаза 2 — Нажатие.** При клике камень «вдавливается» — символы камня смещаются на 1px
вниз-вправо через `transform: translate(1px, 1px)`, трещина визуально расширяется (символы
`│` заменяются на `║` через classList toggle), цвет камня чуть темнеет.

**Фаза 3 — Открытие.** Через 0.3с после нажатия секция стены (5-7 «кирпичей» вокруг
сломанного камня) начинает отъезжать влево — CSS `transform: translateX(-100%)` с
`transition: transform 0.8s ease-in`. За стеной открывается тёмный проём с мерцающим
красным/багровым светом из глубины (факел внутри комнаты администратора).

**Фаза 4 — Вход.** Камера плавно движется в проём (стандартная анимация перехода в
комнату из ТЗ секция 5), и пользователь оказывается в комнате администратора.

### Условие отображения

Сломанный камень виден **только для пользователей с ролью Admin**. Для обычных пользователей
этот участок стены выглядит абсолютно идентично остальной кладке — никаких трещин, никаких
хинтов, никакой пульсации. Проверка роли происходит на уровне TypeScript перед добавлением
интерактивного элемента и CSS-класса `.secret-stone` в DOM. Без класса — обычная стена.

---

## Решение 7. Навигационная карта подземелья (Dungeon Map)

### Суть решения

В подземелье доступна **интерактивная карта** — вид сверху на всё подземелье, показывающий
все комнаты, коридоры, двери и текущее местоположение игрока. Карта — это **основное средство
быстрой навигации** наряду с кликами по дверям и рунами на полу. Работает в двух режимах:
постоянная мини-карта в углу экрана и полноэкранная карта по клавише `M`.

### Визуальная концепция

Карта рисуется в стилистике **roguelike dungeon map** — вид сверху, комнаты как прямоугольники
из box-drawing символов, коридоры как линии, двери как специальные символы. Всё в ASCII,
всё в цветовой теме подземелья. Текущая комната подсвечена ярче всего, посещённые комнаты —
средняя яркость, непосещённые — тусклые.

Подход заимствуется из Asciipocalypse, где миникарта отрисовывается как overlay поверх
3D-сцены. В нашем случае это соответствует top-down карте из ТЗ секция 2, но теперь она
интерактивная — комнаты кликабельны.

```
Карта подземелья (что видит игрок при нажатии M):

    ╔══════════════════════════════════════════╗
    ║          КАРТА ПОДЗЕМЕЛЬЯ                 ║
    ║                                          ║
    ║              ┌─────┐                     ║
    ║              │АДМИН│ (видна только Admin) ║
    ║              └──┬──┘                     ║
    ║                 │                        ║
    ║  ┌─────┐  ┌────┴────┐  ┌──────┐         ║
    ║  │ТРЕН.│──┤ ГЛАВНЫЙ ├──│СТУДИЯ│         ║
    ║  └─────┘  │   ЗАЛ   │  └──────┘         ║
    ║           │  ☺       │                   ║
    ║  ┌─────┐  │  [ТЫ ТУТ]│  ┌──────┐         ║
    ║  │ЭНЦИК│──┤          ├──│СРАВН.│         ║
    ║  └─────┘  └────┬────┘  └──────┘         ║
    ║                 │                        ║
    ║           ┌─────┴─────┐                  ║
    ║  ┌─────┐  │  КОРИДОР  │  ┌──────┐        ║
    ║  │КУРС │──┤           ├──│АНАЛИЗ│        ║
    ║  └─────┘  └─────┬─────┘  └──────┘        ║
    ║                 │                        ║
    ║           ┌─────┴─────┐                  ║
    ║           │ ДАШБОРД   │                  ║
    ║           │(тронный)  │                  ║
    ║           └───────────┘                  ║
    ║                                          ║
    ║  [M] закрыть       [клик] — перейти      ║
    ╚══════════════════════════════════════════╝
```

### Элементы карты и их символы

```
Символ     Значение
──────────────────────────────────────────
☺ или @    Позиция игрока (мерцает, анимирован)
┌─┐└─┘│─  Стены комнат (box-drawing)
├ ┤ ┬ ┴   Дверные проёмы (соединения коридоров)
█████████  Текущая комната (яркая подсветка, --color-primary)
░░░░░░░░░  Посещённая комната (средняя яркость)
▓▓▓▓▓▓▓▓▓  Непосещённая комната (тусклая, намёк на существование)
≋ или ~    Потайная дверь (видна только Admin)
```

### Режим 1 — Minimap (постоянно в углу экрана)

Маленькая карта в правом верхнем углу экрана, размером ~15×10 символов. Показывает только
ближайшие к игроку комнаты и текущую позицию. Реализуется через **Canvas overlay** (как
описано в ТЗ секция 6B — Canvas для high-frequency элементов типа миникарты, с
`pointer-events: none` чтобы клики проходили сквозь к DOM-сцене под ней).

Для максимальной плотности отображения используются **braille-символы** (U+2800–U+28FF) из
ТЗ секция 6A: каждый braille-символ кодирует сетку 2×4 точек = 8 субпикселей. При сетке
миникарты 15×10 символов это даёт эффективное разрешение 30×40 точек — достаточно для
различимой схемы подземелья.

```css
.dungeon-minimap {
    position: fixed;
    top: 16px;
    right: 16px;
    pointer-events: none;
    z-index: 100;
    font-family: var(--font-primary);
    font-size: 10px;
    line-height: 1;
    opacity: 0.7;
}
```

### Режим 2 — Fullmap (полноэкранная, по клавише M)

Полноэкранная карта как overlay поверх 3D-сцены. Фон полупрозрачный (`background:
rgba(0, 0, 0, 0.85)`), карта по центру в `<pre>` элементе. Комнаты кликабельные — каждая
комната обёрнута в `<span class="map-room" data-room="dashboard">`. Клик по комнате
запускает навигацию: закрытие карты → анимация перемещения → открытие двери → контент
комнаты.

По сути fullmap — это **альтернативная навигация** для тех, кто не хочет кликать по дверям
в 3D-виде, а предпочитает видеть структуру целиком и перемещаться через карту.

Открытие/закрытие карты — CSS `opacity` + `transform: scale()` для эффекта «разворачивания
свитка» (0.3с), плюс клавиша `Escape` для закрытия.

### Анимация позиции игрока

Когда игрок переходит между комнатами, его иконка `☺` на карте плавно перемещается по
коридору от одной комнаты к другой через CSS `transition: transform 0.8s ease-in-out`.
Это создаёт ощущение реального перемещения по подземелью, а не мгновенной телепортации.

### Fog of War (опционально, фаза 2)

В продвинутой версии можно добавить «туман войны»: при первом логине видна только стартовая
комната (тронный зал / дашборд). По мере того как пользователь открывает разные разделы,
соответствующие комнаты «раскрываются» на карте. Непосещённые комнаты показываются как `???`
или заполнены `░`. Это добавляет gamification — элемент исследования. Состояние посещённых
комнат хранится в `localStorage` и восстанавливается при следующем входе.

### Техническая реализация

Minimap рисуется на отдельном `<canvas>` или `<pre>` элементе (выбор зависит от частоты
обновления; для статичной миникарты `<pre>` достаточно). Fullmap — DOM-элемент
`<div class="dungeon-fullmap">` с `position: fixed; inset: 0; z-index: 200;`. Содержит
`<pre>` с ASCII-картой, где каждая комната — отдельный `<span>` с `data-room` атрибутом
для обработки кликов.

Горячие клавиши: `M` — toggle fullmap, `Escape` — закрыть fullmap. Обработка через один
`document.addEventListener('keydown', ...)` в `dungeonMap.ts`.

---

## Сводная таблица решений

```
№  Вопрос аудита              Решение                          Приоритет  Фаза
── ──────────────────────────  ───────────────────────────────  ────────── ─────
1  ASCII-заголовок + скелеты  Гравировка-барельеф на стене     Высокий    1
2  Color Picker               Факел-переключатель темы         Средний    1
3  Информация о пользователе  Не применимо (нет данных)        —          —
4  Быстрые действия           Светящиеся руны на камнях пола   Средний    1
5  Страница логина             Ворота подземелья + анимация     Высокий    1
6  Скрытая дверь админки      Сломанный камень в стене         Высокий    1
7  Навигация по карте          Minimap + Fullmap по клавише M   Высокий    1
```

---

## Зависимости от ТЗ

Решения опираются на следующие секции `TZ_DUNGEON_UI.md v1.1`:

```
Секция ТЗ        Используется в решениях
─────────────────────────────────────────────────────
Секция 2          Карта подземелья (top-down) → решение 7
Секция 3          Главный зал, вид от 1-го лица → решения 1, 2, 4, 6
Секция 5          Анимации переходов → решения 5, 6, 7
Секция 6A         Unicode символы (braille для миникарты) → решение 7
Секция 6B         AnsiCell buffer (Canvas overlay) → решение 7
Секция 6C         Font/rendering (@keyframes для факелов) → решения 1, 2, 4
Секция 13         CSS Variables (цветовые темы) → решение 2
Секция 14         Asciipocalypse как архитектурный эталон → все решения
Секция 15         Бенчмарки производительности → решение 7
```

---

## Затрагиваемые файлы кодовой базы

```
Существующие файлы (модификация):
├── ascii-art.ts             → встраивание скелетов в 3D-сцену зала (решение 1)
├── index.html               → добавление gate-page, fullmap overlay (решения 5, 7)
└── navigation.ts            → полная замена на dungeonEngine.ts (все решения)

Новые файлы:
├── ts/dungeonEngine.ts      → основной движок навигации (все решения)
├── ts/dungeonMap.ts         → minimap + fullmap + fog of war (решение 7)
├── ts/dungeonGate.ts        → логика страницы логина-ворот (решение 5)
├── css/dungeon-gate.css     → стили ворот логина (решение 5)
├── css/dungeon-map.css      → стили карты — minimap + fullmap (решение 7)
├── css/dungeon-runes.css    → стили светящихся рун на полу (решение 4)
├── css/dungeon-torch.css    → стили факела-color picker (решение 2)
└── css/dungeon-secrets.css  → стили потайной двери админки (решение 6)
```
