# 🔍 Dungeon UI — Визуальный диагноз
## Что конкретно не так и почему выглядит как хуйня
### Дата: 2026-02-14

---

## TL;DR — 10 проблем, ранжированных по визуальному импакту

Проблемы расставлены от самой критичной к менее важной. Первые 4 — это 80% того,
почему всё выглядит плохо. Остальные 6 — это то, что превратит «нормально» в «охуенно».

---

## 🔴 КРИТИЧНЫЕ (без них всё бессмысленно)

### Проблема 1. Череп-барельеф — позорная заглушка вместо скелетов

**Что вижу:** На задней стене вместо ОХУЕННЫХ детализированных скелетов из `ascii-art.ts`
(336 символов в ширину, 100+ строк, каждая косточка прорисована) — жалкий generic ASCII
череп на 18 строк из `wallRelief.ts`:

```
        _______________
       /               \
      |   / o \   / o \  |
      ...
```

Это как если бы в Skyrim вместо дракона на стене нарисовали палка-палка-огуречик.

**Почему так вышло:** `wallRelief.ts` создаёт свой маленький череп с нуля, полностью
игнорируя существующий арт в `ascii-art.ts`. По дизайн-решению №1 нужно было взять
СУЩЕСТВУЮЩИЕ скелеты из `renderAsciiSkull()` и встроить их в сцену.

**Фикс:**
- Импортировать `renderAsciiSkull()` из `effects/ascii-art.ts`
- Вставить полный арт скелетов на заднюю стену по центру
- Масштабировать через `scaleAsciiSkull()` или CSS `transform: scale()`
- Добавить эффект гравировки (text-shadow для вдавленности)
- Этот арт должен занимать ~60-70% площади задней стены

**Импакт:** ★★★★★ — Это ГЛАВНЫЙ визуальный якорь всей сцены.

---

### Проблема 2. Задняя стена — огромная чёрная дыра

**Что вижу:** Верхняя половина задней стены — чёрная пустота. Кирпичная текстура
покрывает только нижнюю часть, потому что `calcGridSize()` ограничивает сетку до
`Math.min(120, cols)` × `Math.min(60, rows)` — при 14px шрифте это максимум
~840×840px, а стена гораздо больше.

```
┌─────────────────────────────────┐
│                                 │  ← ЧЁРНАЯ ПУСТОТА (нет текстуры)
│                                 │
│                                 │
│▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│  ← кирпичи только тут
│▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│
│ [двери тут внизу]               │
└─────────────────────────────────┘
```

**Почему:** Лимит `Math.min(120, ...)` слишком маленький. Плюс `calcGridSize` берёт
`el.offsetWidth` у элемента с `transform: translateZ(-300px)`, который может возвращать
неправильные размеры из-за 3D-трансформации.

**Фикс:**
- Убрать лимит или поднять до 200×80 минимум
- Использовать фиксированные размеры сетки вместо привязки к DOM-размерам (стена — это
  фиксированный viewport, не респонсивный элемент)
- Для задней стены генерировать минимум 160×70 символов
- Добавить `background: var(--stone-dark)` на `.wall-back` чтобы даже если текстура
  не покроет всё, фон был не чёрным, а тёмно-каменным
- Сделать `overflow: hidden` + `width/height: 100%` на `<pre>` внутри стены

**Импакт:** ★★★★★ — Чёрная дыра убивает всю иллюзию подземелья.

---

### Проблема 3. CRT/Matrix Rain перекрывает всё подземелье

**Что вижу:** Зелёный падающий текст (matrix rain) настолько плотный, что полностью
перекрывает кирпичную текстуру стен, двери, факелы. Подземелье еле видно сквозь дождь.
Это как смотреть на картину через грязное зелёное стекло.

**Почему:** Старый CRT/matrix rain overlay был сделан для плоского UI, где он выглядел
нормально. Но теперь у нас 3D-сцена с собственной визуальной сложностью — два слоя
деталей конкурируют друг с другом, и оба проигрывают.

**Фикс — два варианта:**

Вариант A (рекомендуемый): Сильно уменьшить плотность rain в dungeon-mode:
- `opacity: 0.08-0.12` вместо текущих 0.3-0.5
- Уменьшить количество символов в 3-4 раза
- Или полностью выключить rain внутри `.dungeon-viewport`

Вариант B: Ограничить rain только боковыми стенами (как дымка на периферии):
- Rain видна только на `.wall-left` и `.wall-right`
- Задняя стена, пол, потолок — чистые
- Это создаёт эффект глубины: центр чистый, периферия в дымке

**Импакт:** ★★★★★ — Rain убивает читаемость и атмосферу одновременно.

---

### Проблема 4. 3D-перспектива слишком мелкая — «коробка для обуви»

**Что вижу:** Комната выглядит не как коридор подземелья, а как неглубокая коробка.
Боковые стены слишком узкие (300px), задняя стена слишком близко (translateZ: -300px),
perspective: 800px создаёт слабый эффект глубины.

**Почему:** Значения viewport.css:
```css
perspective: 800px        /* слишком далёкая точка обзора */
.wall-back { translateZ(-300px) }  /* задняя стена слишком близко */
.wall-left/.right { width: 300px } /* боковые стены слишком узкие */
.wall-floor/.ceiling { height: 300px } /* пол/потолок мелкие */
```

В реальном dungeon crawler коридор длинный и уходит в глубину. Сейчас — это шкаф.

**Фикс:**
```css
.dungeon-viewport {
    perspective: 600px;         /* ближе точка обзора = сильнее эффект */
}
.wall-back {
    transform: translateZ(-500px); /* задняя стена дальше = глубже комната */
}
.wall-left, .wall-right {
    width: 500px;               /* шире боковые = больше ощущение объёма */
}
.wall-floor, .wall-ceiling {
    height: 500px;              /* больше пол/потолок */
}
```

Подобрать точные значения экспериментально — ключевое: задняя стена должна быть
заметно МЕНЬШЕ viewport (визуально ~40-50% ширины экрана, не ~70% как сейчас).

**Импакт:** ★★★★☆ — Глубина создаёт всю «подземельную» атмосферу.

---

## 🟡 ВАЖНЫЕ (превращают «нормально» в «хорошо»)

### Проблема 5. Двери — мелкие и невыразительные

**Что вижу:** Дверные рамки — 11 символов шириной, 11 строк высотой. При font-size 16px
каждая дверь ~88×176px. На фоне стены шириной 1000+ пикселей они выглядят как почтовые
ящики, а не как двери в подземелье.

**Фикс:**
- Увеличить DOOR_FRAME минимум до 15×18 символов
- Увеличить font-size `.door-frame` до 18-20px
- Добавить детализацию: петли (`╟`), заклёпки (`•`), текстуру дерева внутри (`║▌│`)
- Signpost сделать крупнее — 16-18px font
- Вертикальные двери: рамка занимает ~30% высоты видимой стены

**Импакт:** ★★★☆☆

---

### Проблема 6. Факелы-switcher — плоский UI-toolbar вместо объектов сцены

**Что вижу:** 5 факелов расположены горизонтально в ряд, как кнопки панели инструментов.
Они «плавают» поверх стены, не интегрированы в 3D-пространство.

**Фикс:**
- Расположить факелы ВЫШЕ дверей, на уровне 60-70% высоты стены
- Каждый факел — над своей дверью или между дверьми
- Увеличить ASCII пламени минимум в 2 раза (текущее `( () ((()) ()  (` — 5 строк, нужно 8-10)
- Добавить radial gradient вокруг каждого активного факела (CSS `radial-gradient`
  от цвета пламени к transparent — создаёт свечение на стене)
- Скрыть неактивные факелы или сделать их тусклыми, показывать все по клику на один

**Импакт:** ★★★☆☆

---

### Проблема 7. Нет освещения — стена равномерно тёмная

**Что вижу:** Кирпичная стена имеет одинаковую яркость по всей площади. Нет эффекта
освещения от факелов — а ведь факелы стоят на боковых стенах! Должны быть яркие пятна
тёплого света возле факелов и темнота между ними.

**Фикс:** Добавить CSS `radial-gradient` поверх стен в местах факелов:
```css
.wall-left::after, .wall-right::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    /* Тёплый свет факела слева по центру */
    background: radial-gradient(
        ellipse 200px 300px at 50% 40%,
        rgba(255, 140, 40, 0.15) 0%,
        rgba(255, 100, 20, 0.05) 40%,
        transparent 70%
    );
}
```

Для задней стены — два пятна света слева и справа (от факелов на боковых стенах,
свет падает на заднюю стену по углам).

Это ОЧЕНЬ сильно меняет атмосферу при минимальных затратах.

**Импакт:** ★★★★☆

---

### Проблема 8. Цветовая палитра — всё сливается в грязь

**Что вижу:** Кирпичи, раствор, пол, потолок — всё в диапазоне #0a-#2a по всем каналам.
Нет контраста, нет теплоты. Реальный каменный подвал имеет тёплые тона от факельного
света.

**Фикс в wallGenerator.ts:**
- Кирпичи: поднять базовые цвета — `0x3a, 0x28, 0x1c` вместо `0x2a, 0x20, 0x18`
- Раствор: сделать ТЕМНЕЕ кирпичей для контраста — `#0f0d0a` на фоне `#0a0908`
- Добавить тёплый offset на все цвета: +10 к red, +5 к green (факельный свет)
- Увеличить диапазон `brightness` — `0.4 + noise * 0.6` вместо `0.6 + noise * 0.4`
  (больше разброс = больше текстуры)
- Пол: поднять яркость на 30-40%, чтобы плиты были видимы

**Импакт:** ★★★☆☆

---

### Проблема 9. Руны на полу — не видны

**Что вижу:** Три символа `⊕ ⊗ ⊘` внизу экрана, очень маленькие, теряются.
Они не выглядят как магические руны на полу.

**Фикс:**
- Увеличить размер рун: font-size 24-32px
- Добавить рунический круг вокруг каждой (box-drawing или braille символы по окружности)
- Увеличить интенсивность glow: `text-shadow: 0 0 20px ...`
- Добавить CSS `transform: perspective(400px) rotateX(50deg)` на контейнер рун,
  чтобы они визуально лежали на полу, а не парили на плоскости экрана

**Импакт:** ★★☆☆☆

---

### Проблема 10. Minimap в углу — слишком мелкая, нечитаемая

**Что вижу:** В верхнем левом углу что-то маленькое и нечитаемое.

**Фикс:**
- Увеличить до минимум 20×12 символов
- font-size 11-12px
- Добавить полупрозрачный фон `rgba(0,0,0,0.7)` с padding
- Добавить рамку из box-drawing `╔═╗║╚═╝`
- Текущая комната — ярким цветом

**Импакт:** ★★☆☆☆

---

## Порядок фиксов (максимальный эффект при минимальных затратах)

```
Шаг  Проблема                    Время     Визуальный эффект
──── ───────────────────────────  ────────  ─────────────────
 1   #3 CRT Rain — убавить       10 мин    Мгновенно видна вся сцена
 2   #2 Back wall — залить       20 мин    Нет чёрной дыры
 3   #7 Освещение — gradients    15 мин    Атмосфера подземелья ×10
 4   #4 Перспектива — углубить   15 мин    Глубина коридора
 5   #8 Палитра — теплее/ярче    20 мин    Контраст и читаемость
 6   #1 Скелеты — из ascii-art   30 мин    Главный визуальный якорь
 7   #5 Двери — увеличить        30 мин    Двери выглядят как двери
 8   #6 Факелы — интегрировать   30 мин    Часть сцены, не toolbar
 9   #9 Руны — перспектива       15 мин    Магия на полу
10   #10 Minimap — увеличить     10 мин    Читаемая карта
                                ────────
                                ~3.5 часа на всё
```

Шаги 1-5 (первые ~80 минут) дадут 80% визуального улучшения.

---

## Сравнение: сейчас vs цель

```
СЕЙЧАС                              ЦЕЛЬ (после фиксов)
──────────────────────────────      ──────────────────────────────
Чёрная дыра наверху                 Полная кирпичная стена
Зелёный дождь скрывает всё          Дождь еле заметный (или выкл)
Плоская коробка                     Глубокий коридор
Грязно-серая каша                   Тёплый камень с факельным светом
Заглушка-череп 18 строк             Два скелета 336×100+ символов
Двери-почтовые ящики                Массивные дубовые двери
Факелы-toolbar                      Факелы на стене со свечением
Руны-пиксели                        Светящиеся руны на полу в перспективе
```
